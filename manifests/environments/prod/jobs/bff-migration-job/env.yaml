apiVersion: batch/v1
kind: CronJob
metadata:
  name: bff-migration-job
spec:
  jobTemplate:
    spec:
      template:
        spec:
          containers:
            - name: bff-migration-job
              env:
                - name: NODE_ENV
                  value: production
